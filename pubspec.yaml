name: myapp
description: Bamberg Ideen App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.2
  firebase_core: ^2.25.4
  firebase_database: ^10.4.5
  cloud_firestore: ^4.15.4
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Ideen_Buergerschaft.csv
